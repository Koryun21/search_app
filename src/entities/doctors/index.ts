import { getSpecialists } from './model/doctors.thunk';

export { default as doctorsReducer } from './model';

export default {
  action: {
  },
  thunks: {
    getSpecialists
  },
  selectors: {
  },
};
